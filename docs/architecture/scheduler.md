# Scheduler Architecture

## Overview

The Scheduler component manages timed execution intervals, cron-like recurring jobs, and automated monitoring triggers for Chawkidaar background loops.

## Integration

The Scheduler interfaces with `LoopEngine` to trigger scheduled runs, periodic health checks, and verification interval checks.
